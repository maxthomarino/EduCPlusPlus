---
import ModuleGroup from "./ModuleGroup.astro";
import type { SidebarModule } from "../../lib/types.js";

interface Props {
  modules: SidebarModule[];
  currentSlug: string;
}

const { modules, currentSlug } = Astro.props;
---

<div class="sidebar-scroll flex h-full flex-col overflow-y-auto">
  <!-- Logo / title + theme toggle -->
  <div class="flex items-center justify-between border-b border-gray-200 px-4 py-4 dark:border-slate-700">
    <div>
      <a href="/" class="flex items-center gap-2 text-lg font-bold text-blue-600 dark:text-blue-400">
        <svg class="h-6 w-6" viewBox="0 0 64 64" fill="none">
          <rect width="64" height="64" rx="12" fill="#3b82f6" />
          <text x="32" y="44" text-anchor="middle" font-family="monospace" font-size="28" font-weight="bold" fill="white">C++</text>
        </svg>
        EduC++
      </a>
      <p class="mt-1 text-xs text-gray-400 dark:text-slate-500">Modern C++20 Reference</p>
    </div>
    <!-- Desktop theme toggle -->
    <button
      id="desktop-theme-toggle"
      class="rounded-md p-1.5 text-gray-400 hover:bg-gray-200 dark:text-slate-400 dark:hover:bg-slate-800"
      aria-label="Toggle dark mode"
    >
      <svg class="h-5 w-5 dark:hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
      </svg>
      <svg class="hidden h-5 w-5 dark:block" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" />
      </svg>
    </button>
  </div>

  <!-- Module list -->
  <nav class="flex-1 px-2 py-3">
    {modules.map((mod) => (
      <ModuleGroup module={mod} currentSlug={currentSlug} />
    ))}
  </nav>
</div>
